const cacher = require('../../')

describe('Framework Validations', ()=>{
    it('should get the result by cache when registed', async ()=>{

        const request = ;
        expect(data._cacher.cached).toBe(true);
    });
})